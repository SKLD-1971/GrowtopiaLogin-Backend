<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Growtopia - Login & Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-100 h-screen flex items-center justify-center">

    <div id="loginBox" class="bg-blue-500 p-8 rounded-lg shadow-lg w-96">
        <h2 class="text-white text-2xl text-center mb-4">Login</h2>
        <form method="POST" action="/login">
            <div class="mb-4">
                <label class="block text-white">GrowID</label>
                <input type="text" name="growid" class="w-full p-2 rounded border focus:outline-none" placeholder="Enter your GrowID">
            </div>
            <div class="mb-4">
                <label class="block text-white">Password</label>
                <input type="password" name="password" class="w-full p-2 rounded border focus:outline-none" placeholder="Enter your Password">
            </div>
            <button type="submit" class="w-full bg-green-500 text-white p-2 rounded hover:bg-green-600">Login</button>
        </form>
        <p class="text-white text-center mt-4">Don't have an account?</p>
        <button onclick="toggleForm()" class="w-full bg-yellow-500 text-white p-2 rounded hover:bg-yellow-600 mt-2">Register</button>
    </div>

    <div id="registerBox" class="bg-blue-500 p-8 rounded-lg shadow-lg w-96 hidden">
        <h2 class="text-white text-2xl text-center mb-4">Register</h2>
        <form method="POST" action="/register">
            <div class="mb-4">
                <label class="block text-white">GrowID</label>
                <input type="text" name="growid" class="w-full p-2 rounded border focus:outline-none" placeholder="Create your GrowID">
            </div>
            <div class="mb-4">
                <label class="block text-white">Password</label>
                <input type="password" name="password" class="w-full p-2 rounded border focus:outline-none" placeholder="Create a Password">
            </div>
            <button type="submit" class="w-full bg-green-500 text-white p-2 rounded hover:bg-green-600">Register</button>
        </form>
        <p class="text-white text-center mt-4">Already have an account?</p>
        <button onclick="toggleForm()" class="w-full bg-gray-500 text-white p-2 rounded hover:bg-gray-600 mt-2">Back to Login</button>
    </div>

    <script>
        function toggleForm() {
            document.getElementById('loginBox').classList.toggle('hidden');
            document.getElementById('registerBox').classList.toggle('hidden');
        }
    </script>

</body>
</html>
